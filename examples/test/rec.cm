def main() {
    let sum = rec_sum(6)
    let fac = rec_fac(5)

    print(sum)
    print(fac)
}

def rec_sum(x: Int) -> Int {
    if x == 0 {
        return 1
    }
    return rec_sum(x - 1) + x
}

def rec_fac(x: Int) -> Int {
    if x == 0 {
        return 1
    }
    return rec_fac(x - 1) * x
}
